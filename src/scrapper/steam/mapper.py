"""
Steam data → DB format transforms (PV-48).

Placeholder for future SQLAlchemy-ready mappings.
Currently the mapping from price rows to FactContainerPrice objects
lives inline in scheduler/tasks.py (backfill_history_task) and cli/database.py.
"""

from __future__ import annotations
